Polymer.HasUtilitiesBehavior = {
//  values: Object.values,
//  reactionValues(room) {
//    return room && room.reactions && this.values(room.reactions) || []
//  },
//  commentValues(room) {
//    return room && room.comments && this.values(room.comments) || []
//  }
//  agreementValues(room) {
//    return room && 
//    function _agreements(comment) {
//    }
//  }
}
